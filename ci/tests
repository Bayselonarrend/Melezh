pipeline {
	agent {
		label 'windows'
	}
    stages {

        stage('Restart Docker Containers') {
            steps {
                script {
                    restartDockerContainers()
                }
            }
        }

        stage('CLI Test (RU)') {
            steps {
                    bat encoding: 'cp866', script:'oneunit e -f ./tests/tests_ru.os'
            }
        }

        stage('CLI Test (EN)') {
            steps {
                    bat encoding: 'cp866', script:'oneunit e -f ./tests/tests_en.os'
            }
        }

        stage('Requests Test') {
            steps {
                    bat encoding: 'cp866', script:'oneunit e -f ./tests/tests_handlers.os'
            }
        }

        stage('Main UI Tests (RPM, RU)') {
            steps {
                bat 'npm install selenium-side-runner --save-dev'
                bat 'npx selenium-side-runner ./tests/selenium/tests.side --base-url http://localhost:1921'
            }
        }
    }
}

def restartDockerContainers() {
    bat """
        cd service\\docker
        docker-compose down --remove-orphans
        docker-compose down --rmi all
        docker-compose pull
        docker-compose up -d --force-recreate
    """
}